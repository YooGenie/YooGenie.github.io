# 정규화
- 중복을 최소화해서 데이터 구조화하는 프로세스
- 데이터 불일치가 발생하지 않도록 데이터 중복을 제거하기 위해서 사용한다
- 데이터를 갱신할 때 한 군데만 갱신하면 된다. => 전체 데이터의 양이 줄어든다
- 종류: 제1정규화, 제2정규화, 제3정규화, 보이스 코드 정규형
- 단점: 검색이 느리다

# 정규 표현식
- 여러 문자열을 하나의 패턴을 식별하는 표현
- 숫자만 사용할 때 [0-9] 이렇게 표현한다

# 트래픽
- 일정 기간 내에 네트워크를 통과하는 데이터의 양(전송량)
- 데이터가 많으면 통신 속도 저하, 네트워크 장애가 발생한다.

# 트리거
- 동작을 시작하는 계기
- 미리 정해 놓은 조건이 만족하거나 어떤 동작을 수행하면 자동으로 수행하는 동작
- 예를 들어서 DB에서 데이터 등록, 삭제가 여기에 포함된다.
- 예시 물품이 등록이 되면 시작 날짜 10시(트리거) => 알람톡이 간다.

# 이벤트
- 프로그램의 내부에서 발생하는 모든 일
- 키보드 엔터를 친다. 버튼을 클릭한다 (이벤트 발생) => 프로그램은 어떤 응답을 한다.
- 자바스크립트에서 onClick() 같은 것


